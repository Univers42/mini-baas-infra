{
  "name": "schema-service",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal schema service scaffold for Docker image build",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "express": "^4.21.2",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.13.14",
    "@types/swagger-ui-express": "^4.1.8",
    "typescript": "^5.8.2"
  }
}
